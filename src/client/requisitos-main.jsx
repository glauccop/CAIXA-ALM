import React from 'react'
import ReactDOM from 'react-dom/client'
import RequirementsApp from './RequirementsApp.jsx'

ReactDOM.createRoot(document.getElementById("requisitos-root")).render(
  <React.StrictMode>
    <RequirementsApp />
  </React.StrictMode>
)